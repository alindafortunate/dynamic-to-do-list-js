// Working with arrays.

let fruits = ['mango', 'banana', 'apple']

// looping through them

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i])
}

// using the shortcut of looping through.
for (let fruit of fruits) {
    console.log(fruit)
}
